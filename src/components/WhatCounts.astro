---
import PixelIcon from './PixelIcon.astro';

const items = [
  {
    icon: 'browser',
    color: 'var(--hc-blue)',
    title: 'full browser',
    body: 'Go big if you want: rendering, tabs, the whole messy thing.',
    example: {
      label: 'Vertex',
      href: 'https://github.com/hackclubium/Vertex',
      blurb: 'a fully from-scratch web browser, written in C++',
    },
  },
  {
    icon: 'extension',
    color: 'var(--hc-purple)',
    title: 'extension',
    body: 'A tab hoarder intervention, a dark-mode-everything button, anything that lives in the toolbar.',
  },
  {
    icon: 'buddy',
    color: 'var(--hc-orange)',
    title: 'browsing buddy',
    body: 'A desktop pet that judges your tabs, a sidebar pal, something that hangs out while you browse.',
  },
  {
    icon: 'weird',
    color: 'var(--hc-red)',
    title: 'something weirder',
    body: "If it touches browsing in any way, it probably counts. Not sure? Ask in Slack.",
  },
];
---

<div class="card-grid">
  {items.map((item) => (
    <div class="card">
      <PixelIcon name={item.icon} color={item.color} />
      <h3>{item.title}</h3>
      <p>{item.body}</p>
      {item.example && (
        <p>
          Example: <a href={item.example.href} target="_blank" rel="noopener">{item.example.label}</a>,
          {' '}{item.example.blurb}.
        </p>
      )}
    </div>
  ))}
</div>
